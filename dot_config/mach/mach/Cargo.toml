[package]
name = "mach"
version = "0.1.0"
edition = "2021"

[dependencies]
sweep = { package = "sweep-engine", path = "../engines/sweep" }
kb = { package = "kb-engine", path = "../engines/kb" }

[[bin]]
name = "mach"
path = "src/main.rs"
